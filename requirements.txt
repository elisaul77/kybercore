fastapi==0.116.1
uvicorn[standard]==0.35.0
Jinja2==3.1.6
aiohttp==3.12.15
websockets==15.0.1
python-multipart==0.0.20
httpx==0.28.1
pytest==7.4.3
pytest-asyncio==1.2.0
requests==2.31.0
python-dotenv==1.0.0

# 🎨 Auto-Plating Dependencies
trimesh==4.5.3        # Manipulación de archivos STL y 3MF
numpy-stl==3.1.2      # Lectura/escritura de STL
rectpack==0.2.2       # Algoritmos de bin-packing 2D
numpy>=2.1.0          # Dependencia de numpy-stl (Python 3.13 compatible)
networkx==3.4.2       # Requerido por trimesh para cargar archivos 3MF
lxml==5.3.0           # Requerido por trimesh para parsear archivos 3MF (XML)

# 🔬 Nesting 3D Avanzado Dependencies
scipy==1.14.1         # Análisis científico (binary_fill_holes, ConvexHull)
scikit-learn==1.5.2   # Machine Learning (DBSCAN clustering, PCA)

# 📄 PDF Processing Dependencies
PyPDF2==3.0.1         # Extracción de contenido de PDFs
Pillow==10.4.0        # Procesamiento de imágenes extraídas de PDFs
pdf2image==1.17.0     # Conversión de páginas PDF a imágenes (requiere poppler-utils)

# 🎨 STL Rendering Dependencies
matplotlib==3.9.2     # Renderizado 3D de STLs a imágenes
pyvista==0.43.10      # Alternativa avanzada para renderizado 3D (opcional)
